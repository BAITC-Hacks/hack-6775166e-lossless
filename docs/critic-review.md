# Независимая проверка MVP «Аким на 5 часов»

## Повторная приёмка интеграции, `main` `5d6e074`

Этот раздел обновляет исторический срез ниже. Код `main` прочитан из основного checkout, без правок чужой ветки. **Лично повторены** `python3 -m unittest discover -s tests -v` (13/13), `node --check web/app.js`, `node --check web/charts.js`, `git diff --check` и отдельный локальный HTTP запуск: база `52.55768`, контрольный план `56.54307`, рекомендация `57.236735` при стоимости `98`. Контрольное объяснение содержит конкретный остаточный риск: у Нуры Score `52,96`, слабейший показатель T2 `40,00`. Чистый клон и браузерную проверку текущего `main` выполнил **интегратор**; сам критик эти две проверки не повторял.

| Прежняя проблема | Статус на `5d6e074` и доказательство |
| --- | --- |
| P1, сравнение вместо перезаписи ручного плана | **Закрыто для MVP.** В `web/app.js` `suggestPlan()` сохраняет рекомендацию отдельно, `renderComparison()` показывает оба Score, стоимость, пятёрки мер, районные различия, критические показатели и компромиссы. Ручной выбор меняется только по явному `adoptPlan()`. Наличие соответствующих узлов подтверждено `web/index.html`; браузерный прогон сообщил интегратор. После применения рекомендации отдельной кнопки возврата к прежнему плану нет — это небольшое ограничение UX, не блокер MVP. |
| P1, конкретность риска в объяснении | **Частично закрыто.** `_facts()` в `src/explanation.py` вычисляет район с минимальным Score и его слабейший показатель; `_render()` принудительно оставляет этот риск и бюджетный компромисс видимыми, даже если модель выбрала другие fact ID. Один новый unit test проверяет плановый риск; API smoke подтвердил T2 `40,00` для контрольного плана. Совет по изменению мер или контрфактическая альтернатива ещё не формируются; в текущем сроке это улучшение P2, а не причина задерживать сдачу. |
| Разнобой округления Score в таблице и графике | **Закрыто по коду.** `web/charts.js` использует `toFixed(2).replace('.', ',')`, как результат в `web/app.js`; `node --check` прошёл. Визуальное совпадение подтвердил интегратор в браузере, не критик. |
| P0, реальный AI-вызов | **Открыто.** Лично повторён `python3 scripts/check-model.py` без ключа: exit 2, `source=computed_facts`, `reason=model_not_configured`. Это корректный fallback, но не свидетельство вызова NVIDIA. Приёмка: зафиксировать `source=model`, `provider=nvidia` на проверочном плане без раскрытия ключа либо честно оставить ограничение в README и демо. |
| P0, финальная воспроизводимость | **Проверена интегратором для текущего `main`, остаётся release gate для окончательного SHA.** Интегратор сообщил чистый клон и браузерный сценарий; я независимо повторил тесты и API. После последнего слияния и перед формой нужно снова сверить именно отправленный commit с удалённым и запустить сценарий по README. |

Вердикт критика: вычислительный MVP и сравнение сценариев готовы для демонстрации на текущем `main`; обязательный AI-анализ нельзя объявлять проверенным модельным ответом до реального NVIDIA-прогона. Рекомендация по приоритету: один контролируемый модельный вызов, затем финальный clean-clone smoke test и push; новых функций до этого не добавлять.

Срез: `agent/critic` от `524b48e`; дополнение к контракту критика в `main` (`c9a5a2f`) прочитано отдельно. Это проверка текущего прототипа, не оценка жюри и не подтверждение финальной версии. Основание: два исходных DOCX в `task/`, `docs/task-requirements.md`, исходный код и команды ниже. Командная база знаний Стефании не настроена (`configured: false`), поэтому не использовалась как источник фактов о задаче.

## Что воспроизведено

- `python3 -m unittest discover -s tests -v`: 12 тестов прошли.
- `python3 benchmark/optimizer_benchmark.py --scenario official --oracle --optimizer python3 src/optimizer.py`: независимый oracle проверил 694395 допустимых портфелей, максимум `57.236735`; оптимизатор вернул тот же план, `regret = 0` (около 11,7 с и 4,0 с в этом запуске).
- Локальный HTTP smoke test на отдельном порту: `/api/catalog` вернул 14 мер и базу `52.55768`; контрольный POST вернул `valid=true`, стоимость `95`, Score `56.54307`; повтор M7 вернул причины отказа **без поля `score`**. Локальное открытие сокета потребовало разрешённого запуска вне песочницы; это ограничение среды теста, а не обнаруженный дефект приложения.
- `python3 scripts/check-model.py` без ключа завершился с кодом 2 и `source=computed_facts`, `reason=model_not_configured`; реальный вызов NVIDIA не проверен. Это поведение честно указано в README.

## Четыре взгляда

### Жюри хакатона

| Критерий ТЗ | Что уже подтверждено | Риск до финальной приёмки |
| --- | --- | --- |
| Соответствие и работоспособность, 25 | Числовой сценарий, валидация и API проходят контрольный пример | Финальная сборка после параллельных правок должна повторить сквозной тест |
| Техническая реализация, 25 | Детерминированный расчёт отделён от LLM; оптимизатор повторно проверяется симулятором | Реальный LLM-прогон не подтверждён; ограничение модели выбором fact ID нужно ясно объяснить как осознанный guardrail |
| README и воспроизводимость, 25 | Есть точные команды, ожидаемые числа, работа без пакетов и ключа | После финального слияния нужен чистый запуск именно удалённого commit |
| Ценность, 15 | Есть понятная бюджетная задача, пять районов и районные дельты | Предложенный план вытесняет пользовательский: сравнение вариантов и цена выбора не видны одновременно |
| Развитие и оригинальность, 10 | Есть точный эталон, графики и проверка на иных входных данных | Не выдавать синтетические эффекты за прогноз города или оптимум Score за общественно оптимальное решение |

Баллы намеренно не выставлены: нет доступа к шкале жюри и финальной версии. Сильный живой показ: ввести контрольный план, увидеть `56.54`, причины риска и районные изменения, затем сравнить с предложенным `57.24`, объяснив, почему различаются распределение и компромиссы. Сейчас шаг сравнения реализован не полностью.

### Заказчик задачи

Пользователь может проверить цену мер и влияние на показатели, но модель эффектов целиком задана синтетическим DOCX. Лондонский CSV проверяет переносимость вычислителя на другие числа, а не правдоподобность эффектов M1–M14 для Лондона или Астаны. Практическая ценность MVP — прозрачное обсуждение сценариев при фиксированных допущениях. Для реального управления позже потребуются калибровка эффектов, диапазоны неопределённости, цена поддержки мер и проверка на городских данных; эти исследования не должны блокировать хакатонный MVP.

### Эксперт по SOTA-подходу

Точный перебор при 14 мерах и пяти решениях является одновременно простым baseline **и доказательным решателем данной конечной задачи**: при полном корректном переборе никакая эвристика не получит больший Score на той же формуле. Сравнение с независимым oracle поддерживает это утверждение для текущих данных. Переход на CP-SAT или MIP имеет смысл при росте числа мер, ограничений или районов, но требует отдельно смоделировать нелинейности `min`, `clip` и порог штрафа, затем сверить с текущим oracle. [Официальная документация OR-Tools](https://developers.google.com/optimization/cp/cp_solver) различает статусы `OPTIMAL` и `FEASIBLE`; поэтому новый метод нельзя называть точным без доказанного `OPTIMAL` и совпадения на контрольных случаях. Для текущего срока выше ценность у проверяемого сравнения двух сценариев и достоверного AI-анализа, чем у замены работающего решателя.

### Эксперт по соответствию конкретному ТЗ

Числовые правила из DOCX совпадают с кодом в проверенных контрольных случаях: 100 единиц, пять разных мер, максимум две на направление, районный/городской scope, конфликты, лаг, синергии, порог `<40`, Score. В формулировке «решения по пяти направлениям» есть двусмысленность; датасет явно разрешает набор 2/1/1/1, поэтому принуждение ровно к одной мере каждого направления было бы необоснованным. Обязательный AI-анализ остаётся **не подтверждён живым модельным вызовом**. Текстовый fallback содержит рассчитанные факты, но его следует показывать именно как fallback, как это уже делает UI.

## Очередь исправлений

| Приоритет и владелец | Наблюдение и доказательство | Критерий готовности |
| --- | --- | --- |
| **P0 — Егор, `agent/explanation`; Саша, интеграция** | `scripts/check-model.py` вернул `source=computed_facts`, exit 2 без ключа; must-have ТЗ требует AI-анализ. Код для NVIDIA есть, подтверждения реального ответа нет. | Егор выполняет один прогон с собственными `NVIDIA_API_KEY`/`NVIDIA_MODEL`: `source=model`, `provider=nvidia`, Score `56.54307`, cost `95`; ключ не попадает в логи/репозиторий. Саша повторно проверяет отображение и фиксирует в README честный итог. Если модель недоступна, не называть fallback модельным анализом. |
| **P0 — Саша, `main`, перед сдачей** | После параллельной разработки предыдущие 12 тестов и HTTP smoke test относятся к срезу `524b48e`, не к будущему финальному commit. | Из чистого клона именно отправляемого commit выполнить README, 12 тестов, контрольный и невалидный HTTP сценарии, проверить браузер, затем сравнить локальный и удалённый SHA. |
| **P1 — Егор, `agent/interface` и `agent/visualization`** | В `web/app.js` `suggestPlan()` делает `state.decisions = proposed` и `showResult(result)`: предыдущий пользовательский план и его Score не остаются для сравнения. Это снижает ценность рекомендации и не закрывает аннотированное пожелание команды. | После расчёта ручного плана кнопка сохраняет его снимок и рядом показывает предложенный: обе пятёрки мер, стоимость, Score, дельта Score, районные выигрыши/потери и один понятный компромисс. Можно применить предложение отдельно; ручной план восстанавливается. Проверить два разных допустимых плана в браузере. |
| **P1 — Егор, `agent/explanation`** | `_facts()` выбирает самый низкий район и общий лаг/остаток, но риск и компромисс могут быть мало связаны с конкретным выбором. Модель сейчас лишь ранжирует заранее сформулированные ID. | Для контрольного плана объяснение называет хотя бы одну конкретную проблему, оставшуюся после выбранных мер, и один вариант улучшения либо цену альтернативы, полученные из вычисленных данных; новые числа модель не сочиняет. Если времени мало, текущий безопасный текст оставить и честно описать предел AI. |
| **P2 — исследование после MVP** | Внешний benchmark подтверждает техническую переносимость, не эмпирическую валидность эффекта мер. | Добавлять реальные данные только с явной лицензией, схемой нормировки и запретом выдавать proxy за измеренную причинность; SOTA-решатель сравнивать с тем же oracle и бюджетом времени. |

Приоритет выполнения: живой AI-прогон и финальный чистый запуск — release gate; сравнение планов повышает ценность, если не задерживает их. Новые алгоритмы и визуальные эффекты после этого срока не окупают риск интеграции.
