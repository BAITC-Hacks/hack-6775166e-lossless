# Preferred model-based selection of verified explanation facts.
# Export these variables in your shell; src/server.py does not load .env files.
NVIDIA_API_KEY=
NVIDIA_MODEL=
# Optional fallback provider when NVIDIA is not configured.
OPENAI_API_KEY=
OPENAI_MODEL=

# Optional local HTTP port (default: 8000).
PORT=8000
